{
  "name": "espresso-addict",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "make-word": "node ./tests/word-tests/_auto-generate-step-definitions.js",
    "make-espresso": "node ./tests/espresso-tests/_auto-generate-step-definitions.js",
    "test-cypress": "cypress run --browser chrome",
    "test-cypress-ui": "cypress open --browser chrome"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "./tests/espresso-tests/step_definitions/**/*.js",
      "./tests/word-tests/step_definitions/**/*.js"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.6",
    "@cypress/webpack-preprocessor": "^6.0.0",
    "cypress": "^13.5.1"
  }
}
