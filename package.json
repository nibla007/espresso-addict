{
  "name": "espresso-addict",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test-cypress": "cypress run --browser chrome",
    "test-cypress-ui": "cypress open --browser chrome"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "./cypress/tests/espresso-tests/step_definitions/**/*.js",
      "./cypress/tests/word-tests/step_definitions/**/*.js"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^19.1.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "@cypress/webpack-preprocessor": "^6.0.0",
    "cypress": "^13.5.1"
  }
}